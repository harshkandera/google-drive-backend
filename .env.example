# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/google-drive-clone

# JWT Configuration (should match NextAuth secret)
JWT_SECRET=Harsh@7877

# AWS S3 Configuration (optional - will use local storage if not provided)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-bucket-name

# Storage Configuration
STORAGE_TYPE=auto  # auto, local, or s3
MAX_FILE_SIZE=104857600  # 100MB in bytes

# CORS
FRONTEND_URL=http://localhost:3000

